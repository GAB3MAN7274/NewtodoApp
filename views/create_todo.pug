extends layout

block content
    form(actions="/add" methods= "post")
        label(for="description") Description
        input(type="text" id="description" name="description")
        button(type="submit") Add Item